---
interface Props {
  showBack?: boolean;
  subtitle?: string;
}

const { showBack = false, subtitle } = Astro.props;
---

<header class="site-header">
  <div class="container header-inner">
    {showBack ? (
      <a href="/" class="back-link">&larr; 回到框架列表</a>
    ) : null}
    <a href="/" class="logo">
      <span class="logo-highlight">PM</span>Frame.works
    </a>
    {subtitle ? <span class="header-subtitle">{subtitle}</span> : null}
    <nav class="header-nav">
      <a href="/roadmap/" class="nav-link">路線圖</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    padding: var(--space-lg) 0;
    border-bottom: 1px solid var(--border);
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
  }

  .logo {
    font-family: var(--font-display);
    font-size: 1.1rem;
    color: var(--text);
    font-weight: 400;
  }

  .logo-highlight {
    font-weight: 600;
    color: var(--accent);
  }

  .back-link {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .back-link:hover {
    color: var(--text);
  }

  .header-subtitle {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-dim);
  }

  .header-nav {
    margin-left: auto;
  }

  .nav-link {
    font-size: 0.85rem;
    color: var(--text-muted);
    padding: 0.3em 0.8em;
    border: 1px solid var(--border);
    border-radius: 20px;
    transition: all 0.2s;
  }

  .nav-link:hover {
    border-color: var(--accent);
    color: var(--accent);
  }
</style>
